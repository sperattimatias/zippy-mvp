export enum TripStatus {
  requested = 'requested',
  negotiating = 'negotiating',
  accepted = 'accepted',
  driver_arriving = 'driver_arriving',
  in_progress = 'in_progress',
  completed = 'completed',
  cancelled = 'cancelled',
  disputed = 'disputed',
}
